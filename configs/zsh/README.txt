#=============#
# ZSH Configs #
#=============#

This folder contains ZSH scripts that are loaded by .zshrc on initialization

Any file ending in `.ignored.zsh` will be ignored by version control

Files prefixed with `_` will be loaded first
