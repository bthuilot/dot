#=============#
# Git Configs #
#=============#

This folder contains git configs (and related files) for configuring
git installation. 

These are included in the main git config file via the templater
run by the git install script
